# Git LFS is not enabled for this initial private upload.
# Checkpoints are below GitHub's 100 MB hard file limit, so they are committed as regular files.
# If this repository becomes public long term, move model weights to Git LFS or GitHub release assets.
